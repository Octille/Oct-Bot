const { DataResolver } = require("discord.js")

module.exports = {
    name: 'kick',
    description: "To kick a member do '!kick (USERNAME)'",
    execute(client, message ) {

    },
    help: 'Kicks memebers, to kick a member !kick <UserName>'
    }
    